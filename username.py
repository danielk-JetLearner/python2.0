while True:
    user=input ("what is your full name?")
    print(user.split())
    name=user.split()
    if user.lower()=='exit':
        break
    
    if len(name)<2:
        continue
    name[0]
    first = name[0]
    last = name[1]
    first.lower()
    last.lower()
    print(first)
    print(last)
    daniel = first[0:4] + last[0:3] + "35"
    print(daniel)